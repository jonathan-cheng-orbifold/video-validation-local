# Wasabi config
WASABI_BUCKET=insta360-videos
WASABI_REGION=ap-northeast-1
WASABI_ENDPOINT=https://s3.ap-northeast-1.wasabisys.com

# Path INSIDE the container (important)
WASABI_CREDENTIALS_CSV=/run/secrets/wasabi_credentials.csv

# Auth
AUTH_SECRET=password123
AUTH_COOKIE_SECURE=false
AUTH_COOKIE_SAMESITE=lax
AUTH_TTL_SECONDS=43200
